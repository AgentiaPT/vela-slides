# Vela Slides — part-file concatenation order (SINGLE SOURCE OF TRUTH).
# Read by tools/vela-dev/scripts/concat.py + lint.py (via vela_manifest.py)
# and by tests/test_vela.py. One filename per line, in fixed dependency
# order; text after `#` on a line is a comment (the part's routing purpose).
# Changing this order changes the build — the order matches the dependency
# graph and must not be reordered casually.
part-imports.jsx   # constants, version/changelog, capability detection, cost tracker, startup patch, storage API
part-imports-sanitize.jsx # sanitizers — string/url/color/style/SVG encoders + allowlists (security surface)
part-imports-sanitize-deck.jsx # deck-ingress key allowlists, slide/block/deck sanitizers, study-notes sanitizer
part-imports-ui.jsx # image compression, status meta, themes, logo, style factories, CSS generator, prompt constants
part-icons.jsx     # 270+ Lucide icon resolver
part-blocks-inline.jsx # error boundary, EditableText, inline **md** formatting, glossary links
part-blocks-item.jsx   # per-item machinery — hover chrome, link/delete/reorder, item sub-components
part-blocks.jsx    # RenderBlock — the 27-type block renderer switch
part-blocks-branding.jsx # BrandingOverlay (logo, footer, slide numbers)
part-blocks-layout.jsx # SlideContent layout composer, inline comment card, split-image layout
part-reducer.jsx   # useReducer state + dispatch actions
part-engine.jsx    # Vera AI engine core — callClaudeAPI(), shared API helpers
part-engine-tools.jsx # tool execution engine (22 tools) + system prompts
part-engine-modes.jsx # teacher mode, slide design API, timing estimation, chat step, SSE recovery, TOC slide generator
part-slides.jsx    # slide canvas core — VirtualSlide, layout stats, ScopeSelector, BrandingPanel, CinemaTip
part-slides-presenter.jsx # fullscreen presenter chrome — PresenterTOC, GalleryView/Thumb, CommentPopover, PresenterView, TeacherMessage
part-slides-student.jsx # student mode — StaticStudyPanel (offline study notes), StudentPanel dispatcher, TeacherPanel, SectionPicker
part-slides-panel.jsx # SlidePanel component — fullscreen/nav/keyboard, Present Edit, Quick/Block Edit, Generate Slide, Alternatives
part-list.jsx      # lane/module list, drag-and-drop
part-chat.jsx      # ChatPanel, tool traces
part-test.jsx      # battery render tests
part-uitest.jsx    # 185 UI tests in 33 suites
part-demo.jsx      # cinematic demo mode (18 scenes)
part-pdf.jsx       # canvas PDF export, DOM→canvas capture, vector extraction
part-pdf-fonts.jsx # embedded PDF fonts — zlib-compressed base64 TTF blobs (~242KB)
part-pdf-vector.jsx # vector PDF build, font pipeline, markdown + standalone HTML export
part-pptx.jsx      # native editable PowerPoint (.pptx) export — DrawingML builders, ZIP writer, public entry
part-pptx-extract.jsx # PPTX DOM extraction — text/SVG/table/image capture from rendered slides
part-app-modals.jsx # Modal Backdrop (shared), PPTX Export Modal, Deck Stats Dialog, Changelog Dialog
part-app-panels.jsx # Comments Panel, New Deck Dialog, Keyboard Shortcuts Overlay, Session Cost Badge, Agent status chip + settings dialog
part-app-merge.jsx # Item Fingerprint + Merge Patch Dialog (startup-patch merge detection)
part-app.jsx       # root VelaApp component — layout, storage load/save, startup patch, keyboard handlers, mobile
