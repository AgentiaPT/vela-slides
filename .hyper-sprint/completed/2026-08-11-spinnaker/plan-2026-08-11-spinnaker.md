# Sprint "spinnaker" — plan (2026-08-11)

## Verbatim original prompt

> use hyper sprint to tacke the follow change requests
>
> for the ones that are not clear yet, you should use a pm/ux sub agent to nail down the actual requirements
> security is #1 priority at all times

Change requests (from user's screenshots, transcribed):

1. Ability to remove the brand line on top, currently can't, even when I use 0 px the line is still visible on top
2. Move branding config panel to a side pane on the right, current layout/position doesn't feel professional
3. While showing [presenter], the gallery view icon on the top right is not visible?
4. Similar, the edit icon doesn't always show properly when in presenter mode (top right), sometimes it shows but often it does not, not sure why
5. Export in html, when in neutralino at least (maybe others), is giving error: `unknown: Identifier 'useState' has already been declared. (5:8)` — exported HTML contains both the app's React destructuring and the Neutralino UMD shim (generated by sync-vela.py)
6. A feature like gallery view shouldn't be hidden. Research what's a good UX option to show/cycle between presenter | editor | gallery view. We should highlight this. Current overview button below the slide doesn't feel the proper place. Should be shown near the present mode.
7. On the TOC while in editor view, there should be an icon to delete the slide, like we can do with the sections; currently we can only hide/unhide
8. When image fills the slide, the item/block toolbar gets cut on top and it's very hard to use/click; needs research on the best UX alternative
9. Neutralino app title just shows "Vela Slides"; it should also show the deck title — "Vela Slides - <Deck Title>", never repeating "Vela Slides"
10. Sometimes links on text blocks are not showing at the proper place — overlapping final text/words, or very far to the right; stress test and ensure it shows perfect in all cases
11. When the Neutralino window loses focus and I get back, the keyboard is not working until I click inside the window; expect keyboard to work immediately on refocus (alt-tab). Affects presenter, editor, gallery.
12. Bug: with Neutralino, every time I have a new build and start the app, AI integration is always "offline" even with a rescan; restart fixes it. Root cause and fix.

Constraints: PM/UX sub-agent decides requirements for unclear CRs (2, 6, 8). Security is #1 priority at all times.

## Stop rule (from .hyper-sprint/config.md)

Blind best-model hunt ≥3 min (engine-enforced deadline) finds no in-scope bugs + all
features present, and a frame-checked recorded demo deck exists. Proof also includes the
Markdown sprint report in this folder.

## Phase status

- Phase 0a intake: done (this file)
- Phase 0b readiness: delegated (background)
- Phase 1 recon: delegated (recon-app, recon-neutralino) + PM/UX requirements (CR2/6/8)
- Phase 2 clustering: TBD after recon returns (draft below)
- Phase 3 delegate & integrate: pending
- Phase 4 fix-round hunt: pending
- Phase 5 blind gate + proof + archive: pending

## Draft clustering (to be finalized after recon)

- Cluster A — Branding (CR1, CR2): part-branding.jsx + branding config UI. One worker.
- Cluster B — Presenter/view toolbar (CR3, CR4, CR6): part-slidepanel.jsx, part-slides.jsx. One worker (file overlap with each other, disjoint from A).
- Cluster C — TOC delete (CR7): part-list.jsx + part-reducer.jsx. One worker.
- Cluster D — Block toolbar clipping (CR8): part-canvas.jsx. One worker.
- Cluster E — Link chip positioning (CR10): part-blocks.jsx. One worker.
- Cluster F — Neutralino (CR5, CR9, CR11, CR12): vela-neutralino/** + part-export-md.jsx + possibly part-imports.jsx. One worker (desktop expertise clustered; CR5 export fix may split if file-disjoint).

Model routing: workers sonnet by default; Neutralino cluster opus (root-cause fixes);
validators fable/max (blind gate); recon sonnet/opus as dispatched.

## What happened vs plan

(appended at close)
